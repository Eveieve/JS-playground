import User from './user.js';
//make sure to use ./ when importing things in JS

const user = new User('Bob', 11);
console.log(user); //console logs the user object! //although it appears as User and not user!
